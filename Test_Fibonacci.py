from Fibonacci import Fibo

class test_Fibo(Fibo):
    def cal(self, Input_number):
        for Input_number in range(2, Input_number):
            temp = self.Fibo_number[Input_number -1 ] + self.Fibo_number[Input_number - 2]
            temp != 0, "List is Empty"

Fibo = []

